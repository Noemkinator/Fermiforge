{
  "name": "fermiforge-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:wasm": "wasm-pack build ../core --release --target web --out-dir ../web/wasm-pkg",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "svelte": "^4.2.19",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "wasm-pack": "^0.13.1"
  }
}
